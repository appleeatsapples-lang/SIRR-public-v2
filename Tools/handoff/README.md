# Tools/handoff/

Orchestrator brief write target. New session-handoff briefs and orchestration artifacts land here.

## History

Historical briefs through 2026-04-25 (PR-5 era and earlier — pre-scrub `Tools/handoff/`) are archived in:

```
ARCHIVE/repo_pre_scrub_2026-04-25.tar.gz
```

under `Tools/handoff/` inside the tarball. The 30-day tarball window is the recovery path; if a specific historical brief is needed, extract into `PRIVATE/` (never into `REPO/`) — those briefs predate the privacy scrub and may carry pre-scrub identifiers that must not re-enter public git history.
